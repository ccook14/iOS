//: Playground - noun: a place where people can play

import UIKit
// A set stores disinct values of the same type of a collection with no defined ordering. You can use a set instead of an array when the order of items is not import. or you need to ensure the item only appears once.


var letters = Set<Character>()

letters.insert("c")

var favs: Set<String> = ["Pizza", "Wine", "Mozz Sticks", "Chicken Parm"]
